#pragma once

#include <vector>
#include <functional>
#include <string>

IsBlackListed :: IsBlackListed(BloomFilter* BloomFilter) : bloomFilter(bloomFilter){}

void IsBlackListed :: execute(const std::string& url){
    //implement
}
    