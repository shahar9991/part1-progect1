#include <iostream>
#include <string>
#include <bitset>
#include <functional>
#include <vector>
using namespace std;

class create_bloom_filter : public ICommand{
    public:
    void execute(input) {
        int array_size = input[1]
        int hash1 = input[2]
        int hash2 = input[3]

        bitArray.resize(array_size, false); // Initialize the bit array with false values
        
    }
}