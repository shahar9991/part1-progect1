#include "../src/string.cpp" // Include the code to be tested
 #include <iostream> 

int main() {
    int size;
    std::vector<int> args;

    // Call the function to read input from the user
    readInputFromUser(size, args);

    return 0;
}